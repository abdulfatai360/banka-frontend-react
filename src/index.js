import React from 'react';
import ReactDOM from 'react-dom';
import '@Assets/styles/index.css';

ReactDOM.render(
  <div>Welcome to Banka, a lightweight core banking application</div>,
  document.getElementById('root')
);

module.hot.accept();
